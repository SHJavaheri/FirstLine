# Backend Folder

Core backend logic is implemented in `src/backend` and consumed by Next.js API routes in `src/app/api`.

This folder exists to satisfy the requested high-level project structure for a split frontend/backend architecture.
